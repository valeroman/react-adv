Crear el proyecto react-app
    npx create-react-app react-app --template typescript

    reconstruir el proyecto yarn

    correr el proyecto yarn start

React-Router-Dom
    doc => https://v5.reactrouter.com/web/guides/quick-start
    yarn add react-router-dom
    yarn add -D @types/react-router-dom

Comandos git
    git add .
    git commit -m "Proyecto Inicial Listo"
    git branch
    git branch -M main

    si borramos un archivo y lo queremos recuperar
        git checkout -- .

    crear una nueva rama
        git checkout -b lazyload

        git checkout -b lazyload-nested

        si hago git checkout lazyload, regreso a mi proyecto como estaba sin el lazyload-nested

    ver los url de mi proyecto en git
        git remote -v

    subir las ramas lazyload-nested y lazyload
        git push da error, hay que usar este Comandos

        git push --set-upstream origin lazyload-nested

        git checkout lazyload

        git push

        git push --set-upstream origin lazyload

        git checkout -b component-compound

        git checkout -b component-extend-styles



    